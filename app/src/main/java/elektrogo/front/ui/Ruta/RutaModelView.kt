package elektrogo.front.ui.Ruta

import com.google.android.gms.maps.model.LatLng
import kotlinx.coroutines.runBlocking

class RutaModelView {

   /* fun sendRouteInfo(latLngOrigin: LatLng, latLngDestination:LatLng, autonomia:Int) : tipusretorn = runBlocking {
          val statusCodes = FrontendController.sendRouteInfo(latLngOrigin.latitude, latLngOrigin.longitude, latLngDestination.latitude,latLngDestination.longitude,autonomia)
    }*/
}